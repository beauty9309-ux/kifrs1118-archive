# K-IFRS 제1118호 아카이브 — 1주차 구축 계획

두 축(**A 개정 지도** / **B 쟁점 해설**) 콘텐츠를 Claude Code로 채우고 배포까지 하는 일주일 실행 계획 + 복붙용 프롬프트.

> 준비된 자산: `CLAUDE.md`, `shared.css`, 예시 3종(홈·쟁점·개정), 스킬 2종(`kifrs-issue-page`·`kifrs-amendment-page`), `check-copyright.js` + pre-commit 훅.

---

## 1. 시작 전 준비 (Day 0 · 약 30분)

- [ ] 저장소에 파일 배치:
  ```
  /  CLAUDE.md  shared.css  kifrs1118-home.html  kifrs1118-*.html  check-copyright.js
  /.claude/skills/kifrs-issue-page/SKILL.md
  /.claude/skills/kifrs-amendment-page/SKILL.md
  /.githooks/pre-commit
  ```
- [ ] 훅 켜기: `chmod +x .githooks/pre-commit && git config core.hooksPath .githooks`
- [ ] Claude Code를 **저장소 루트**에서 열고 `/memory` 로 CLAUDE.md·스킬 로드 확인.
- [ ] Vercel 연결 — 콘텐츠가 적어도 파이프라인부터 살려둔다(배포하며 작업).
- [ ] 배포 구조 결정: **평면**(파일 그대로) vs **하위폴더**(`/issues`, `/amendments`). 하위폴더로 가면 각 페이지의 `href="shared.css"` 를 `href="/shared.css"` 로 바꾼다.

---

## 2. 한 페이지 표준 절차 (SOP) — 매번 이 5단계

1. **원문 확보** — 회계기준원(kasb.or.kr)에서 대상 자료(PDF/텍스트) 다운로드.
2. **핵심 추출** — A: `"1118" 검색 → 부칙 개정근거(C1x) → 개정 대상 문단`. B: `쟁점 정의 → 갑설·을설 → 결론 → 핵심 해설`.
3. **페이지 생성** — 해당 스킬로 예시 페이지 복제 + 본문만 교체(§4 프롬프트).
4. **자가검사** — `node check-copyright.js <파일>` 통과 + 모바일·링크 확인.
5. **연결·커밋** — 홈 피드 1줄 + 인덱스에 추가 → 커밋(훅 자동검사) → 배포.

---

## 3. 일자별 계획 (유연하게 — 밀리면 Day 7 버퍼로)

| Day | 주제 | 산출물 목표 |
|---|---|---|
| 1 | 뼈대 & 배포 | 인덱스 2개(`/issues`·`/amendments`) + `sitemap.xml`·`robots.txt` + Vercel 라이브 + Search Console 등록 |
| 2 | A 집중(가벼운 개정) | 참조·명칭 정비류 batch — 3~5장 |
| 3 | A 집중(무거운 개정) | 제1007호 현금흐름표 1장 제대로 + 중간 난이도 1~2장 |
| 4 | B 집중 | 보험회사 대출 쟁점 + 질의회신 기반 1~2장 |
| 5 | 개요 & 연결 | 1118호 개요 1장 + 페이지 상호링크 + 홈 피드 정리 |
| 6 | SEO & 소개 | 메타·OG·JSON-LD 점검 + About·저작권 페이지 + sitemap 갱신·제출 + 재배포 |
| 7 | 버퍼 & 회고 | 밀린 것 마무리 + "어떻게 만들었나" 회고 메모(면접용) + 2주차(AI) 계획 |

**주말 목표 산출물:** 홈 + 인덱스 2 + A 5~7장 + B 3~4장 + 개요 + About/저작권 = **배포된 MVP**.

---

## 4. A축 우선 후보 (⚠ 원문 부칙에서 1118 개정 여부 확인 후 진행)

- **무거움(예상):** 제1007호 현금흐름표
- **중간(예상):** 제1033호 주당이익 · 제1034호 중간재무보고 · 제1108호 영업부문
- **가벼움(예상, batch):** 제1115호(완료 예시) · 그 밖 참조·명칭 정비류

> "예상"은 확정이 아니다. **반드시 각 기준서 원문 부칙의 개정 근거로 확인**하고 영향도를 판정한다.

---

## 5. 복붙용 프롬프트 (Claude Code)

> 프롬프트는 짧게 유지한다 — 규칙·디자인·절차는 `CLAUDE.md`와 스킬이 이미 갖고 있다. (토큰 절약)

### 5-1. A 개정 지도 페이지
```
kifrs-amendment-page 스킬로 제[번호]호 개정 지도 페이지를 만들어줘.
원문은 [경로/첨부].
먼저 원문에서 '1118'을 찾아 개정 근거 문단과 개정 대상 문단을 확인하고,
실질 개정인지 참조·명칭 정비인지 판정해서 영향도(낮음/중간/높음)를 정해.
kifrs1118-amendment-1115.html 을 복제해 본문만 교체하고, shared.css 링크는 그대로 둬.
문단 전문은 복제하지 말고 번호+요지만, 원문은 회계기준원 링크로.
끝나면 node check-copyright.js 로 검사하고, 홈 피드와 개정 지도 인덱스에 각각 1줄 추가해줘.
```

### 5-2. B 쟁점 해설 페이지
```
kifrs-issue-page 스킬로 [쟁점명] 쟁점 해설 페이지를 만들어줘.
원문은 [경로/첨부] (회의 회차·일자 포함).
쟁점을 한 문장으로 정의하고, 갑설·을설(·병설)과 각 근거 문단, 결론(다수/소수)을 원문에서 뽑아.
'왜 그 결론인지, 어느 문단 관계에서 갈렸는지'를 핵심 해설로 쉽게 풀어줘.
kifrs1118-customer-financing-interest.html 을 복제해 본문만 교체하고 shared.css는 그대로.
문단 전문 복제 금지(번호+요지), 원문은 회계기준원 링크.
끝나면 check-copyright.js 검사 + 홈 피드·쟁점 인덱스에 1줄씩 추가.
```

### 5-3. 인덱스(목록) 페이지 — Day 1
```
쟁점 인덱스 페이지(issues-index.html)와 개정 지도 인덱스 페이지(amendments-index.html)를 만들어줘.
- shared.css를 링크하고 홈/콘텐츠 페이지와 같은 디자인 언어로.
- 각 항목: 제목 + 카테고리(쟁점) 또는 영향도(개정) + 날짜 + 링크.
- 지금 존재하는 페이지만 나열하고, 새 페이지가 추가되면 여기에 한 줄씩 늘리는 구조로.
- lang="ko", 각자 title·description·canonical 포함.
```

### 5-4. 홈 피드 업데이트 (페이지 추가 후)
```
방금 만든 [파일명]을 kifrs1118-home.html 의 '최근 업데이트' 피드 맨 위에 한 줄 추가해줘.
카테고리 배지([쟁점]/[개정])와 날짜를 붙이고, 기존 항목 형식 그대로.
```

### 5-5. sitemap / robots — Day 1
```
현재 저장소의 모든 배포용 .html을 대상으로 sitemap.xml 과 robots.txt를 만들어줘.
도메인은 [실제 도메인]. sitemap에는 각 페이지의 loc만 넣고, robots.txt는 전체 허용 + sitemap 위치 명시.
새 페이지 추가 시 내가 sitemap을 다시 생성해달라고 하면 갱신해줘.
```

### 5-6. 전체 QA / 저작권 검사 — Day 6
```
저장소의 모든 .html을 점검해줘.
1) node check-copyright.js 를 전체 대상으로 돌려 결과를 보여줘.
2) 각 페이지에 title·meta description·canonical·Open Graph·JSON-LD·하단 저작권 고지가 다 있는지 표로 확인.
3) 깨진 내부 링크, 모바일 반응형 문제가 있으면 목록으로.
고칠 건 바로 고치지 말고 먼저 목록으로 보여줘.
```

### 5-7. 1118호 개요 페이지 — Day 5
```
kifrs1118-* 콘텐츠 페이지와 같은 디자인으로 1118호 개요 페이지(overview.html)를 만들어줘.
회계기준원 공개자료를 근거로 '왜 개정됐나 / 무엇이 바뀌나(개괄) / 시행일·전환'을 정리하되,
기준서 전문 복제 없이 요지+링크로. 두 축(개정 지도·쟁점 해설)으로 가는 안내를 넣어줘.
```

---

## 6. 페이지 완료 기준 (Definition of Done)

- [ ] 예시 페이지 복제 기반 + `shared.css` 링크 (인라인 CSS 없음)
- [ ] 문단 전문 복제 없음 → 번호 + 자기표현 요지 + 회계기준원 링크
- [ ] 결론/영향도 배너, 근거 문단, 하단 출처·저작권·면책 고지 포함
- [ ] `title`·`description`·`canonical`·OG·JSON-LD(`Article`+`BreadcrumbList`)·`lang="ko"`
- [ ] 모바일에서 깨지지 않음
- [ ] `node check-copyright.js <파일>` 통과
- [ ] 홈 피드 + 해당 인덱스에 추가됨
- [ ] **회계사인 내가 내용을 읽고 검증함** (AI 초안 ≠ 검증 완료)

---

## 7. 저작권 3원칙 (항상)

1. 기준서 문단 **전문 복제 금지** → 번호 + 자기표현 요약 + 회계기준원 링크.
2. **회계기준원 공개자료만** 본문 근거. 4대 법인 자료·유튜브·인강은 **링크만**.
3. 모든 페이지 하단에 **출처·저작권·면책** 고지.

---

## 8. 리듬 & 주의

- **"매일"이 아니다.** 하나씩, 밀리면 버퍼(Day 7)로. 죄책감 프로젝트로 만들지 않는다.
- **정확성 > 개수.** 내가 검증하지 못한 페이지는 올리지 않는다.
- **애매하면 원문 확인.** 기억으로 추측하지 않는다.
- 인덱스를 손으로 갱신하는 게 번거로워지는 순간이 **2주차 Next.js 이전 신호**다(목록·검색·사이트맵 자동화).
