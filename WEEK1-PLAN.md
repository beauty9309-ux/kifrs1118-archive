# K-IFRS 제1118호 아카이브 — 1주차 구축 계획

두 축(**A 개정** / **B 쟁점 해설**) 콘텐츠를 Claude Code로 채우고 배포까지 하는 일주일 실행 계획 + 복붙용 프롬프트.

> 준비된 자산: `CLAUDE.md`, `shared.css`, 예시 3종(홈·쟁점·개정), 스킬 3종(`kifrs-issue-page`·`kifrs-amendment-map`·`kifrs-amendment-page`), 소스(`sources/`), `check-copyright.js` + pre-commit 훅.

> **A축 방침(중요):** 40개 기준서를 페이지로 쪼개지 않는다. A = **개정 지도 1장(전체 영향도 표)** + **실질 개정 소수만 상세 페이지**. 소스는 기준서 원문이 아니라 마스터 문서 하나(`sources/1118-consequential-amendments.txt`).

---

## 1. 시작 전 준비 (Day 0 · 약 30분)

- [ ] 저장소에 파일 배치:
  ```
  /  CLAUDE.md  shared.css  kifrs1118-home.html  kifrs1118-*.html  check-copyright.js
  /sources/  1118-consequential-amendments.txt  tf-3rd-meeting.txt  standards/
  /.claude/skills/kifrs-issue-page/SKILL.md
  /.claude/skills/kifrs-amendment-map/SKILL.md
  /.claude/skills/kifrs-amendment-page/SKILL.md
  /.githooks/pre-commit
  ```
- [ ] **`sources/`는 작업용 — 배포/공개에서 제외**(`.gitignore` 또는 배포 ignore). 공개되는 건 요약·해설 페이지지 원문 덤프가 아니다.
- [ ] 훅 켜기: `chmod +x .githooks/pre-commit && git config core.hooksPath .githooks`
- [ ] Claude Code를 **저장소 루트**에서 열고 `/memory` 로 CLAUDE.md·스킬 로드 확인.
- [ ] Vercel 연결 — 콘텐츠가 적어도 파이프라인부터 살려둔다(배포하며 작업).
- [ ] 배포 구조 결정: **평면**(파일 그대로) vs **하위폴더**(`/issues`, `/amendments`). 하위폴더로 가면 각 페이지의 `href="shared.css"` 를 `href="/shared.css"` 로 바꾼다.

---

## 2. 한 페이지 표준 절차 (SOP)

1. **소스 확보** — A: `sources/1118-consequential-amendments.txt`(마스터, 하나). B: 해당 TF/질의회신 소스(`sources/…`).
2. **핵심 추출**
   - A(지도): 마스터에서 개정 대상 목록 추출 → 영향도(경미/실질) 분류.
   - A(상세): 마스터의 **해당 기준서 섹션**에서 개정 근거 문단·바뀐 문단 확인(기준서를 따로 뒤지지 않는다). 불분명하면 '대조본 확인 필요'로 두고 추측 금지.
   - B: 쟁점 정의 → 갑설·을설 → 결론 → 핵심 해설.
3. **페이지 생성** — 해당 스킬로 예시 페이지 복제 + 본문만 교체(§5 프롬프트).
4. **자가검사** — `node check-copyright.js <파일>` 통과 + 모바일·링크 확인.
5. **연결·커밋** — 홈 피드/개정 지도 행/인덱스 갱신 → 커밋(훅 자동검사) → 배포.

---

## 3. 일자별 계획 (유연하게 — 밀리면 Day 7 버퍼로)

| Day | 주제 | 산출물 목표 |
|---|---|---|
| 1 | 뼈대 & 배포 | 쟁점 인덱스(`/issues`) + `sitemap.xml`·`robots.txt` + Vercel 라이브 + Search Console 등록 |
| 2 | A — 개정 지도 | 마스터 문서에서 **개정 지도 1장**(전체 영향도 표, `/amendments` 인덱스 겸용) |
| 3 | A — 실질 상세 | 제1007호 현금흐름표 등 **실질 개정 소수만** 상세 2~4장 |
| 4 | B 집중 | 보험회사 대출 쟁점 + 질의회신 기반 1~2장 |
| 5 | 개요 & 연결 | 1118호 개요 1장 + 페이지 상호링크 + 홈 피드 정리 |
| 6 | SEO & 소개 | 메타·OG·JSON-LD 점검 + About·저작권 페이지 + sitemap 갱신·제출 + 재배포 |
| 7 | 버퍼 & 회고 | 밀린 것 마무리 + "어떻게 만들었나" 회고 메모(면접용) + 2주차(AI) 계획 |

**주말 목표 산출물:** 홈 + 쟁점 인덱스 + **개정 지도 1장 + A 실질 상세 3~5장** + B 3~4장 + 개요 + About/저작권 = **배포된 MVP**.

> A가 가벼워진 만큼 남는 시간은 **B(쟁점)** 에 쓴다. 진짜 대체 불가능한 가치는 B다.

---

## 4. A 처리 방식 — 지도 1장 + 실질 소수 상세

- **개정 지도(전체 1장):** 마스터 문서 하나로 약 40개 기준서를 영향도 표로 요약. **대부분 경미**(용어 IFRSs→IFRS Accounting Standards, 참조 제1001호→제1118호, 명칭 제1008호→'재무제표의 작성 기준')는 **표의 행으로만**, 별도 페이지 없음.
- **실질 상세(소수만 별도 페이지):** 예상 후보 — 제1007호 현금흐름표 · 제1034호 중간재무보고 · 제1033호 주당이익 · 제1012호 법인세 · 제1105호 매각예정·중단영업.

> "예상"은 확정이 아니다. **마스터 문서의 개정 근거로 실질 여부를 확인**하고, 마스터만으로 불분명하면 '대조본 확인 필요'로 두고 지어내지 않는다.

---

## 5. 복붙용 프롬프트 (Claude Code)

> 프롬프트는 짧게 유지한다 — 규칙·디자인·절차는 `CLAUDE.md`와 스킬이 이미 갖고 있다. (토큰 절약)

### 5-1a. A 개정 지도 (전체 1장) — Day 2
```
kifrs-amendment-map 스킬로 1118호 개정 지도(전체 1장)를 만들어줘.
소스는 sources/1118-consequential-amendments.txt 하나.
개정 대상 기준서를 뽑아 영향도(경미/실질)로 분류해 표로 요약해:
 - 경미(용어·참조·명칭 정비): 표에 요지만, 별도 페이지 없음.
 - 실질(표시·측정·공시 로직 변경): 표에 표시하고 상세 페이지로 링크(나중에).
마스터만으로 불분명하면 '확인 필요'로 두고 지어내지 마.
shared.css 링크, amendments-map.html (=/amendments 인덱스 겸용). 끝나면 check-copyright.js.
```

### 5-1b. A 개정 상세 (실질 소수) — Day 3
```
kifrs-amendment-page 스킬로 제[번호]호 개정 상세 페이지를 만들어줘.
소스는 sources/1118-consequential-amendments.txt 의 '제[번호]호' 섹션(기준서를 따로 뒤지지 마).
섹션 끝 개정 근거 문단에서 바뀐 문단을 뽑고, 신구대조(앞=신/뒤=구)로 변경 내용을 읽어.
문단별로 '확정 가능 / 대조본 확인 필요'를 나누고, 불분명하면 지어내지 마.
영향도(중간/높음) 판정 후 kifrs1118-amendment-1115.html 복제·본문 교체(shared.css 유지).
번호+요지만(BC·적용사례·소수의견은 요약만), 원문은 회계기준원 링크.
끝나면 check-copyright.js + 개정 지도 표의 해당 행 링크 + 홈 피드 1줄.
```

### 5-2. B 쟁점 해설 페이지
```
kifrs-issue-page 스킬로 [쟁점명] 쟁점 해설 페이지를 만들어줘.
소스는 sources/tf-3rd-meeting.txt (또는 해당 회의 회차·일자 원문).
쟁점을 한 문장으로 정의하고, 갑설·을설(·병설)과 각 근거 문단, 결론(다수/소수)을 뽑아.
'왜 그 결론인지, 어느 문단 관계에서 갈렸는지'를 핵심 해설로 쉽게 풀어줘.
kifrs1118-customer-financing-interest.html 을 복제해 본문만 교체하고 shared.css는 그대로.
문단 전문 복제 금지(번호+요지), 원문은 회계기준원 링크.
끝나면 check-copyright.js 검사 + 홈 피드·쟁점 인덱스에 1줄씩 추가.
```

### 5-3. 쟁점 인덱스 페이지 — Day 1
```
쟁점 인덱스 페이지(issues-index.html)를 만들어줘.
(개정은 amendments-map.html 이 /amendments 인덱스를 겸하므로 개정 인덱스는 따로 안 만든다.)
- shared.css를 링크하고 홈/콘텐츠 페이지와 같은 디자인 언어로.
- 각 항목: 제목 + 카테고리(쟁점) + 날짜 + 링크. 존재하는 페이지만, 추가 시 한 줄씩.
- lang="ko", title·description·canonical 포함.
```

### 5-4. 홈 피드 업데이트 (페이지 추가 후)
```
방금 만든 [파일명]을 kifrs1118-home.html 의 '최근 업데이트' 피드 맨 위에 한 줄 추가해줘.
카테고리 배지([쟁점]/[개정])와 날짜를 붙이고, 기존 항목 형식 그대로.
```

### 5-5. sitemap / robots — Day 1
```
현재 저장소의 모든 배포용 .html을 대상으로 sitemap.xml 과 robots.txt를 만들어줘.
도메인은 [실제 도메인]. sitemap에는 각 페이지의 loc만 넣고, robots.txt는 전체 허용 + sitemap 위치 명시.
sources/ 는 배포/색인에서 제외. 새 페이지 추가 시 다시 생성해달라고 하면 갱신해줘.
```

### 5-6. 전체 QA / 저작권 검사 — Day 6
```
저장소의 모든 .html을 점검해줘.
1) node check-copyright.js 를 전체 대상으로 돌려 결과를 보여줘.
2) 각 페이지에 title·meta description·canonical·Open Graph·JSON-LD·하단 저작권 고지가 다 있는지 표로 확인.
3) 깨진 내부 링크, 모바일 반응형 문제가 있으면 목록으로.
고칠 건 바로 고치지 말고 먼저 목록으로 보여줘.
```

### 5-7. 1118호 개요 페이지 — Day 5
```
kifrs1118-* 콘텐츠 페이지와 같은 디자인으로 1118호 개요 페이지(overview.html)를 만들어줘.
회계기준원 공개자료를 근거로 '왜 개정됐나 / 무엇이 바뀌나(개괄) / 시행일·전환'을 정리하되,
기준서 전문 복제 없이 요지+링크로. 두 축(개정 지도·쟁점 해설)으로 가는 안내를 넣어줘.
```

---

## 6. 페이지 완료 기준 (Definition of Done)

- [ ] 예시 페이지 복제 기반 + `shared.css` 링크 (인라인 CSS 없음)
- [ ] 문단 전문 복제 없음 → 번호 + 자기표현 요지 + 회계기준원 링크
- [ ] 결론/영향도 배너, 근거 문단, 하단 출처·저작권·면책 고지 포함
- [ ] `title`·`description`·`canonical`·OG·JSON-LD(`Article`+`BreadcrumbList`)·`lang="ko"`
- [ ] 모바일에서 깨지지 않음
- [ ] `node check-copyright.js <파일>` 통과
- [ ] 홈 피드 + (A 상세면) 개정 지도 행 / (B면) 쟁점 인덱스에 추가됨
- [ ] **회계사인 내가 내용을 읽고 검증함** (AI 초안 ≠ 검증 완료)

---

## 7. 저작권 3원칙 (항상)

1. 기준서 문단 **전문 복제 금지** → 번호 + 자기표현 요약 + 회계기준원 링크.
2. **회계기준원 공개자료만** 본문 근거. 4대 법인 자료·유튜브·인강은 **링크만**.
3. 모든 페이지 하단에 **출처·저작권·면책** 고지. `sources/` 원문은 배포에서 제외.

---

## 8. 리듬 & 주의

- **"매일"이 아니다.** 하나씩, 밀리면 버퍼(Day 7)로. 죄책감 프로젝트로 만들지 않는다.
- **정확성 > 개수.** 내가 검증하지 못한 페이지는 올리지 않는다.
- **애매하면 원문 확인.** 기억으로 추측하지 않는다. 마스터만으로 불분명하면 '대조본 확인 필요'로 남긴다.
- 쟁점 인덱스·개정 지도 행을 손으로 갱신하는 게 번거로워지는 순간이 **2주차 Next.js 이전 신호**다(목록·검색·사이트맵 자동화).
